import React, { Component } from 'react';
import SearcBar from '../containers/search_bar'

export default class App extends Component {
  render() {
    return (
      <div>
        <SearcBar/>
      </div>
    );
  }
}
